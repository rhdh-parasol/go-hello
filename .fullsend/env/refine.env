# Sandbox env for the refine agent.
# expand: true uses Go os.ExpandEnv (${VAR} only — no bash :- defaults).

export ISSUE_KEY="${ISSUE_KEY}"
export ISSUE_SOURCE="${ISSUE_SOURCE}"
export REPO_FULL_NAME="${REPO_FULL_NAME}"
export GITHUB_ISSUE_URL="${GITHUB_ISSUE_URL}"
export HUMAN_INSTRUCTION="${HUMAN_INSTRUCTION}"
